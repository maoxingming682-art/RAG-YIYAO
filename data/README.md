# Data Files

Large generated data, vector indexes, model checkpoints and forecast outputs are not committed to git.

This repository keeps only small support files:

- `drug_aliases.csv`
- `rag_dialog_eval_cases.json`

To run the full local demo, restore or regenerate the large files under `data/`, especially `chunks.json` and `vectors.npy`.
